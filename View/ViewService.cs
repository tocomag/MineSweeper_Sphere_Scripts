public class ViewService
{
    private BoardView boardView;
    private BoardSettings stgs;
    public ViewService(BoardSettings stgs, BoardView boardView)
    {
        this.stgs = stgs;
        this.boardView = boardView;
    }
    // Viewに対する処理を書いていく
}